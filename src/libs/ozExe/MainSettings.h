#pragma once
#include "ozExe.h"

#include <QObject>

class OZEXE_EXPORT MainSettings : public QObject
{
    Q_OBJECT
public: // types

public: // ctors
    explicit MainSettings(QObject *parent = nullptr);

public slots:

signals:

public: // const

public: // non-const

private:

};
