#pragma once

#include <QObject>

class ErrorManager : public QObject
{
    Q_OBJECT
public: // ctors
    explicit ErrorManager();

public slots:

signals:

public: // const

public: // non-const

public: // non-const

private:

};
